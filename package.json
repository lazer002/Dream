{
  "name": "dream-shop",
  "private": true,
  "version": "0.1.0",
  "description": "Full-stack e-commerce (Node/Express, MongoDB/Mongoose, React/Tailwind) with admin panel and guest cart",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n \"api,ui\" -c blue,green \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "build": "npm run build --workspace backend && npm run build --workspace frontend",
    "start": "npm run start --workspace backend",
    "seed": "npm run seed --workspace backend"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
